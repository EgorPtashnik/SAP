<core:FragmentDefinition
    xmlns:core="sap.ui.core"
    xmlns="sap.m">
    <Table items="{/lists}"
           noDataText="No Todo Lists"
           mode="{=${/deleteMode} ? 'Delete' : 'SingleSelectMaster'}"
           delete="onOpenDeleteConfirmationDialog"
           busy="{config>/busy}"
           busyIndicatorDelay="1">
        <columns>
            <Column>
                <Text text="Name"/>
            </Column>
            <Column>
                <Text text="Status"/>
            </Column>
            <Column>
                <Text text="Category"/>
            </Column>
        </columns>
        <items>
            <ColumnListItem>
                <cells>
                    <ObjectIdentifier title="{name}" text="Items: {ItemsCount}"/>
                    <Text text="{path: 'category_id', formatter: '.Formatter.getCategoryText'}"/>
                    <MessageStrip text="{path: 'status', formatter: '.Formatter.getListStatusText'}"
                                  type="{path: 'status', formatter: '.Formatter.getListStatusType'}"/>
                </cells>
            </ColumnListItem>
        </items>
    </Table>
</core:FragmentDefinition>